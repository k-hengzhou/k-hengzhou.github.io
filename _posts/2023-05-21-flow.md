---
layout: post
title: "光流法"
categories: cv
description: "光流法原理介绍"
keywords: vo vslam optical-flow
---
## 光流法

### 光流

光流是由物体或相机的运动引起的图像物体在两个连续帧之间的明显运动模式。
它是 2D 矢量场，其中每个矢量都是一个位移矢量，显示点从第一帧到第二帧的移动。
如图\ref{pic1}（图片提供：维基百科关于光流的文章）。

<img src="/images/post/optical_flow_basic.jpg" width="50%" alt="optical flow"  style="center"/>

考虑第一帧像素$I(x,y,t)$,在下一帧沿向量移动了$(dx,dy)$,两帧间隔$dt$,假设这些
像素在两帧之间像素灰度不变

$$
I(x,y,t)=I(x+dx,y+dy,t+dt)
$$

对等号右边进行一阶泰勒展开，可得

$$
dx*I_x+dy*I_y+dt*I_t=0
$$

等式同时除以$dt$可得

$$
\frac{dx}{dt}I_x+\frac{dy}{dt}I_y+I_t=0
$$

令$u=\frac{dx}{dt}$和$v=\frac{dy}{dt}$
可得光流等式

$$
u*I_x+v*I_y+I_t=0
$$

### LK光流

光流不等式是一个二元一次方式，为了能求解出$u$和$v$,假设像素的邻域和像素同运动,
LK光流假设$3\times 3$的方块内像素同运动.这样我们就可以得到九个光流等式，通过
最小二乘法计算得到$u$和$v$

### 灰度误差最小匹配

假设相邻两帧的灰度函数分别为$I_1(x,y)$和$I_2(x,y)$,最小化

$$
E=\min\|I_1(x,y)-I_2(x+dx,y+dy)\|^2
$$

在使用高斯-牛顿方最优化时如果使用$I_1(x,y)$在$(x,y)$处的导数取代$I_2(x,y)$在$(x+dx,y+dy)$
处的导数就叫做反向光流法。使用反向光流法的好处是不用每次迭代是都重复计算雅可比矩阵。

在光流等式中取$dt=1$和$I_t=I_1(x,y)-I_2(x+dx,y+dy)$就可以得到和上式同样的优化过程。
所以上式是光流等式的离散形式。


为了避免优化公式陷入局部最优解，常使用多层金字塔来优化函数即

$$
E=\min\sum_i\|I_{i1}(x,y)-I_{i2}(x+dx,y+dy)\|^2
$$

其中$I_{i1}$和$I_{i2}$分别为第一帧和第二帧金字塔第$i$层的灰度函数

